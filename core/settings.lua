-- core/settings.lua
SETTINGS = {
    VOXEL_POOL_SIZE = 6000,
    ZOOM = 1,
    FOCAL_LENGTH = 50,
    Z_OFFSET = 50,
    CAMERA_SPEED = 800,
    MIN_CAMERA_SPEED = 650,
    NEAR_PLANE = 0.1,
    FAR_PLANE = 500,
    SIDEBAR_WIDTH = 250,
    TOPBAR_HEIGHT = 50,
    KEYS = {
        UP    = "w",
        DOWN  = "s",
        LEFT  = "a",
        RIGHT = "d",
        ERASE = "lshift",
        FULLSCREEN = "f"
    }
}
